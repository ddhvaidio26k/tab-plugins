// Minimal type stub for standalone plugin.
// Replace with your full Supabase types if needed.
export type Json = string | number | boolean | null | { [key: string]: Json | undefined } | Json[];
export type Database = Record<string, any>;